// Place your settings in this file to overwrite default and user settings.
{
    "files.exclude": {
        "**/*.js": {
            "when": "$(basename).ts"
        },
        "**/*.map": true,
        "src/**/*.d.ts": true,
        "**/*.css": {
            "when": "$(basename).scss"
        }
    }
}